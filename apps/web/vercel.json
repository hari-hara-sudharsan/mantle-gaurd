{
    "buildCommand": "cd ../.. && HUSKY=0 pnpm install --frozen-lockfile && pnpm run build --filter=web",
    "devCommand": "pnpm run dev",
    "installCommand": "HUSKY=0 pnpm install --frozen-lockfile",
    "framework": "nextjs",
    "outputDirectory": ".next"
}